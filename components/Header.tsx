import React from "react";

type HeaderProps = {
  setOpenMenu: React.Dispatch<React.SetStateAction<boolean>>;
};
export default function Header({ setOpenMenu }: HeaderProps) {
  return <div></div>;
}
